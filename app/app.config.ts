export default defineAppConfig({
  ui: {
    colors: {
      primary: "blue",
      neutral: "grey",
    },
    button: {
      slots: {
        base: "button justify-center",
      },
    },
  },
});
